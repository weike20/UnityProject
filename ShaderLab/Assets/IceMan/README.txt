这是一个初级的冰块渲染Shader。并不保证在所有设备上表现正常，所以需要你自己进行兼容性测试。当然还有相当的优化空间，比如可以将多张纹理进行合并，以便减少纹理采样次数；MainTex甚至是可以去掉的，这样又能减少一次纹理采样；Fragment中的部分计算还能再进行简化，以减少指令数量。这些都需要使用者自己去完成了。其中有一张 LUT 纹理，是通过 GenerateSkinLUT 这个脚本离线生成的。